#include "push_swap.h"

stack *initialize_stack() {
    // Inicializa e retorna uma nova pilha
}

int parse_arguments(int argc, char **argv, stack *a) {
    // Analisa os argumentos e preenche a pilha a
}

int is_valid_stack(stack *a) {
    // Verifica se a pilha é válida
}

int is_sorted(stack *a) {
    // Verifica se a pilha está ordenada
}

int stack_size(stack *a) {
    // Retorna o tamanho da pilha
}

void free_stack(stack *s) {
    // Libera a memória alocada para a pilha
}